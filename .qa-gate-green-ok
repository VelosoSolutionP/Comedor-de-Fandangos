EXCECAO AO QA-GATE — registrada por impedimento de FERRAMENTA, nao por opcao.

O gate oficial NAO rodou. Motivo: o servidor MCP "veloso-solution" falhou ao
conectar nesta sessao, entao qa_run_gate esta indisponivel (ToolSearch
confirma: "these configured MCP servers failed to connect"). Nao existe
recibo .git/qa-gate-green.json e NINGUEM deve tratar esta entrega como
"gate verde".

O que foi executado no lugar, contra banco e servidor REAIS (PostgreSQL 14 +
WildFly 26 + JDK 11, subidos localmente em .local/ por falta de WSL para o
Docker):

  suite                        verificacoes   resultado
  ---------------------------------------------------------------
  backend  DocumentosTest              10     10/10 passou
  front    validadores.test.js         55     55/55 passou
  banco    db/test/smoke.sql           24     24/24 passou
  api      scripts/smoke-api.sh        39     39/39 passou
  front    app.e2e.js (jsdom)          35     35/35 passou
  front    produtos.e2e.js (jsdom)     23     23/23 passou
  ---------------------------------------------------------------
  total                              186     186/186 passou

Reproduzir:  ./scripts/testar-tudo.sh

Cobre os dois modulos: cadastro de clientes e catalogo de produtos.

O que NAO foi validado: `docker compose up -d --build`. O Docker Desktop desta
maquina nao sobe o engine (WSL nao instalado, e habilitar exige admin +
reboot). Dois dos bugs corrigidos nesta branch afetavam diretamente o build da
imagem (jboss-cli e dialeto do Hibernate), mas o caminho Docker continua sem
execucao. Rodar o gate de verdade numa maquina com Docker operante segue
PENDENTE e esta endereçado ao tech lead em docs/DOC-ENTREGA.md.
